# react-scripts 5 declares an outdated optional peer range for TypeScript
# (^3.2.1 || ^4), but this project uses TypeScript 5. That mismatch makes
# `npm ci` fail its strict peer check even though the build works fine, so we
# opt into legacy peer resolution to keep local installs and CI consistent.
legacy-peer-deps=true
